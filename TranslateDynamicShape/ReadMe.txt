Maya软件的命令开发(Maya软件的动态链接库) 两个不一样的拓扑的模型,将他们的UV展平了完全重叠在一起,然后就可以根据这个UV来传递这两个模型的动态 用法:先要复制一个本体,并且将复制的这个名称改成 本体名称+"_Base" 的形式,这个是用来计算动态差值用的,然后先选择本体再选择目标体,执行TranslateDynamicShape 0.1命令就可以将本体的单帧动态传递给目标体了(注意这里传递的是动态,也就是发生变形后的本体与"..._Base"这个本体之间的差值);这个命令后面的0.1是容差(tolerance)(根据UV查模型点的容差)
